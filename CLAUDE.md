# seatmap-demo — 作業ルール

仕様書: `~/seatmap-demo-spec/` — 入口は `ROUTING.md`。その段階が指定した文書だけを読む。

## 不変ルール(違反したら即中断して報告)

1. **キャンバスに個人座席カードを描かない。** キャンバス = 通路線 + チーム箱 + 家具/会議室。座席は `.sr-only` ミラーレイヤーにのみ置く
2. 個人カード(アバター・氏名・在席状態)は**チームオーバーレイモーダルの中だけ**でレンダーする
3. 座標は全て `mocks/*.json` から来る。コードへの座標ハードコード禁止
4. DOM フック固定: チーム箱インタラクション面 = `data-team-id`、パンズームレイヤー = `data-canvas-transform-layer="true"`、会議室 = `data-furniture-id` + `data-facility="true"`。検証スクリプトがこのフックに依存する
5. 1段階 = 1セッション。指定された仕様書以外は開かない

## 完了条件(3つ全て満たして初めて「完了」と言える)

- `npx tsc --noEmit` 通過
- `scripts/verify-s1.js` を実行中の画面で走らせて `verdict: "PASS"`
- プッシュ後 GitHub Pages 配信版でも同じスクリプトが PASS

ローカル PASS だけで完了と言わない。

## コミット規則

**形式**: `<type>: <日本語の要約>` + 本文は箇条書き(日本語・簡潔)

- **type**: `feat`(機能追加) / `fix`(バグ修正) / `hotfix`(緊急修正) / `refactor`(リファクタ)
- 要約は1行・日本語。本文は `- ` の箇条書きで変更点だけ
- **AI署名を付けない**: `Co-Authored-By` も `Claude-Session` も書かない

例:
```
feat: チームバウンダリクリックで大型オーバーレイを開く

- 座席は sr-only ミラーのみ、クリックはオーバーレイを開く
- ポインタ状態機械(TAP 8px / 長押し 300ms)
- クリック位置から拡大、座席クリックで社員詳細をスタック
```

**タイミング**: 意味のある単位(機能/ドキュメント1つ)が完成したら自動でコミット。毎回確認しない。

## コメント規則

- 日本語コメントは最小限。自明なものは書かない
- 必要な所だけ短い名詞句で(長い説明文は禁止)

## コードスタイル

- セミコロンなし / シングルクォート / `any`・`unknown` 禁止(3rd-party・catch・type guard 除く)
- hooks は `use` 接頭辞、Props 型はファイル上部
- import alias: 兄弟 `./` / 同ツリー `../` / ツリー跨ぎ `@/`

## ファイル構成

1ファイルが 200 行を超え、かつ責務が複数あるなら次の形へ分ける。

```
components/<Name>/
  index.tsx     組み立てのみ
  type.ts       型は平坦にここへ(サブフォルダを切らない)
  components/   下位部品
  lib/          フック
  utils/        純関数・定数
```

- `pages/*.tsx` はルート専用。画面本体は `components/<Name>/` へ置き、ページは Head とプロバイダだけにする
- 単一責務のまま長いだけのモジュール(1つのフック等)は分割しない
